<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.laughing.kotlin">

    <uses-permission android:name="android.permission.INTERNET"/>
    <!--android:name=".kotlin.test.MyApplication"-->
    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">

         <activity android:name=".kotlin.MainActivity">
         </activity>
        <activity android:name=".kotlin.DataBindingActivity">
        </activity>
        <activity android:name=".kotlin.CollectionActivity">
        </activity>
        <activity android:name=".kotlin.InnerClassActivity">
        </activity>
        <activity android:name=".anko.activity.ShopCarActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>